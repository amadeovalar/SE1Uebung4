package at.aau.serg.exercises.shapetesting;

import java.util.Iterator;

public class main {
    public static void main(String[] args) {
        
        
        // int side = 10;
        // Square squareSides = new Square(side);
        // Rectangle rectangleSides = new Rectangle(side, side);
        
        // System.out.println(rectangleSides.getSides());
        // System.out.println(squareSides.getSides().iterator());
        
        // Iterator<Side> iter = squareSides.getSides().iterator();

        // while (iter.hasNext()) {
        //     System.out.println("All the sides are " + iter.next().getLength());
        // }


    
    }

}